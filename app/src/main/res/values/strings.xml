<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">Student Planner</string>

    <!--
      No longer used by MainActivity.java — the app now loads the
      bundled static site from app/src/main/assets/www/ (see
      MainActivity.LOCAL_HOME_URL) instead of a hosted PHP URL, so it
      works fully offline. Left here only in case a hosted backend is
      wanted again later; nothing reads this value right now.
    -->
    <string name="app_url">https://black35.infinityfreeapp.com/login.php</string>
</resources>
